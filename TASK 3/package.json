{
  "name": "task-3",
  "version": "1.0.0",
  "description": "Taks 3- Asynchronous File Handling that reads a text file named \"data.txt\" and counts the number of words in it. The script should print the total word count to the console.",
  "main": " ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sukrit Sahu",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security"
  }
}
